# Normalize line endings to LF in the repository for every text file
# (matches .editorconfig / prettier endOfLine=lf). Windows-only scripts keep CRLF.
* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
