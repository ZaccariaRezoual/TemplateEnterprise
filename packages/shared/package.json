{
  "name": "@enterprise/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic utilities and validation schemas shared by apps and packages. Contains no Vue and no DOM assumptions, so it is reusable from any runtime.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@enterprise/types": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
