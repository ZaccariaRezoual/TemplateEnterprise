# Enforce the Node/pnpm versions declared in package.json (engines/packageManager).
engine-strict=true
# Workspace packages always link via workspace: protocol to avoid accidental registry installs.
save-workspace-protocol=rolling
