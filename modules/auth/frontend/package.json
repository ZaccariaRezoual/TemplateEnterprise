{
  "name": "@enterprise/module-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frontend of the Auth module: session store, route guard, login/register/account pages. Plugs into the host app through the seams core exposes (token provider, unauthorized handler); the app never implements auth itself.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@enterprise/sdk": "workspace:*",
    "@enterprise/shared": "workspace:*",
    "@enterprise/ui": "workspace:*",
    "zod": "catalog:"
  },
  "peerDependencies": {
    "pinia": "^4.0.0",
    "vue": "^3.5.0",
    "vue-router": "^5.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:",
    "@vue/test-utils": "catalog:",
    "jsdom": "catalog:",
    "pinia": "^4.0.3",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "vue": "catalog:",
    "vue-router": "^5.2.0",
    "vue-tsc": "catalog:"
  }
}
