{
  "name": "@enterprise/module-authorization",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frontend of the Authorization module: the permissions store, the usePermissions composable and the v-can directive. Lets any feature hide what the user cannot do, while the API remains the actual security boundary.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@enterprise/sdk": "workspace:*",
    "@enterprise/shared": "workspace:*"
  },
  "peerDependencies": {
    "pinia": "^4.0.0",
    "vue": "^3.5.0",
    "vue-router": "^5.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:",
    "@vue/test-utils": "catalog:",
    "jsdom": "catalog:",
    "pinia": "^4.0.3",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "vue": "catalog:",
    "vue-router": "^5.2.0",
    "vue-tsc": "catalog:"
  }
}
