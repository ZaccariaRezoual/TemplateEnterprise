# Copy to .env and adjust if needed. All values have working local defaults.
# LOCAL DEVELOPMENT ONLY — never reuse these values in any deployed environment.

POSTGRES_USER=app
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=enterprise
POSTGRES_PORT=5432

REDIS_PORT=6379

# Seq UI + ingestion (http://localhost:5341) — login: admin / SEQ_ADMIN_PASSWORD
SEQ_PORT=5341
SEQ_ADMIN_PASSWORD=dev_password
